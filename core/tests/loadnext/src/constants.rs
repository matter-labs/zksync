use std::time::Duration;

pub const ETH_CONFIRMATION_TIMEOUT: Duration = Duration::from_secs(60);
pub const COMMIT_TIMEOUT: Duration = Duration::from_secs(600);
pub const POLLING_INTERVAL: Duration = Duration::from_secs(3);
