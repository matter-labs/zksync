// Signer. TODO: describe what's here.

/*
struct Signer {
    private_key
}

impl Signer {
    fn new(private_key) -> Signer;
    fn new_from_seed(seed: &[u8]) -> Signer;

    // See models::node::tx::Transfer
    fn sign_transfer(&self, ...args) -> Transfer;

    fn pubkey_hash(&self) -> PubKeyHash;
}
 */
