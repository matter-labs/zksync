{
	"name": "zksync-crypto",
	"version": "0.0.0",
	"browser": "pkg/web.js",
	"main": "pkg/node.js",
	"scripts": {
		"build": "wasm-pack build --out-name=web && wasm-pack build --target=nodejs --out-name=node && rm pkg/package.json",
		"test": "echo \"Error: no test specified\" && exit 1"
	}
}
