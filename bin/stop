#!/bin/bash

PROCESS_ID=`ps aux | grep target/release/server | grep -v grep | grep -o -E '[0-9]+'  | head -1 | sed -e 's/^0\+//'`

if [ ! -z "$PROCESS_ID" ]
then
    kill -9 $PROCESS_ID
    echo "Server stopped (PID $PROCESS_ID)"
else 
    echo "No server running"
fi