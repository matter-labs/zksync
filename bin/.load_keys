#!/bin/bash

echo "Checking keys in $KEY_DIR/"
mkdir -p $KEY_DIR

for i in $KEY_FILES; do
    if ! [ -f $KEY_DIR/$i ]; then
        echo "Downloading $SPACE_URL/$i to $KEY_DIR/"
        curl -o $KEY_DIR/$i $SPACE_URL/$i
    fi
done