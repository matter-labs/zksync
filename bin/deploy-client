#!/bin/bash

SRC=`dirname $0`/../js/client
DST=$CLIENT_GITHUB_DIR

echo "$DST <= $SRC"

cd $SRC
yarn run build
cp index.html $DST
cp dist/* $DST/dist
cd $DST
git add -A
git commit -m "deploy"
git push --force
